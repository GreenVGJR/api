import { ParsedResponse, ParsedResponseJsonDataFile, ParsedSingularResponseFile, SingularResponseFile } from '../types/parsed-response';
/**
 * Merges the parsed singular responses with the JSON data for multiple parallel requests.
 * - Singular responses are outputted to separate files, JSON data is outputted to stdout
 */
export declare const mergeResponsesWithJsonData: (singularResponses: ParsedSingularResponseFile[], jsonResponses: ParsedResponseJsonDataFile[]) => ParsedResponse[];
/**
 * Parses a single requests response file from multiple requests.
 * - Contains headers and response body only
 * - JSON data is outputted separately in stdout
 */
export declare const parseSingularRequestResponse: (requestResponse: SingularResponseFile) => ParsedSingularResponseFile;
/**
 * Parses JSON data for multiple parallel requests.
 */
export declare const parseMultipleResponsesJsonData: (requestResponse: string) => ParsedResponseJsonDataFile[];
//# sourceMappingURL=multiple-requests-parser.d.ts.map