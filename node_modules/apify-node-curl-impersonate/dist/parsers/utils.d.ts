import { ParsedHeaders, ParsedResponseJsonData } from '../types/parsed-response';
import { RequestResponse } from '../types/response';
/**
 * Splits the response into headers and the rest of the response (may only be body or body and JSON data).
 */
export declare const splitRequestHeadersAndRest: (rawResponse: string) => {
    headers: string;
    rest: string;
};
/**
 * Parses the response headers into an object.
 * - Assigns the `set-cookie` header as an array of strings (there can be multiple `set-cookie` headers).
 */
export declare const parseResponseHeaders: (headersString: string) => ParsedHeaders;
export declare const pickResponseJsonData: (jsonResponse: RequestResponse) => ParsedResponseJsonData;
//# sourceMappingURL=utils.d.ts.map