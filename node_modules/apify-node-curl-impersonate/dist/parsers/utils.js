"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pickResponseJsonData = exports.parseResponseHeaders = exports.splitRequestHeadersAndRest = void 0;
/**
 * Splits the response into headers and the rest of the response (may only be body or body and JSON data).
 */
const splitRequestHeadersAndRest = (rawResponse) => {
    const lastHeadersObject = getLastHeadersObject(rawResponse);
    const rest = rawResponse.split(lastHeadersObject)[1];
    return { headers: lastHeadersObject, rest };
};
exports.splitRequestHeadersAndRest = splitRequestHeadersAndRest;
/**
 * Finds the index of the last headers object in the response.
 * - The response might've been redirect, if such then there will be multiple headers objects in sequence.
 * - Each headers object is separated by an empty line. They looks like this:
 *     ```log
 *     HTTP/1.1 301 Moved Permanently
 *     Server: nginx
 *     Content-Type: text/html
 *     ```
 * - The last headers object is followed by the response body, so we have to make sure we stop matching headers there
 */
const getLastHeadersObject = (rawResponse) => {
    /**
     * Matches the last headers object in the response.
     * - `^(?<!\n)` makes sure we start at the beginning of the file.
     * - `HTTP.+\d\s.+\n` matches the first line of the headers object.
     * - `.*:\s.*\n` matches all of the headers in the object (matches "key: value").
     * - The 2nd group will have the last headers object
     */
    const LAST_HEADERS_OBJECT_REGEX = /^(?<!\n)((HTTP.+\d\s.+\n(.*:\s.*\n)*\n))+/m;
    const match = rawResponse.match(LAST_HEADERS_OBJECT_REGEX);
    if (!match)
        throw new Error('Invalid response format');
    // Prevent NodeJS's regex string memory leak. Not sure if it leaks here, but we had a problem with it in the past.
    // https://stackoverflow.com/questions/30332959/memory-leak-in-node-regex-parser
    return match[1].split('').join('');
};
/**
 * Parses the response headers into an object.
 * - Assigns the `set-cookie` header as an array of strings (there can be multiple `set-cookie` headers).
 */
const parseResponseHeaders = (headersString) => {
    const headers = {};
    headersString.split('\n').forEach((header) => {
        const [key, value] = header.split(': ');
        const isSetCookieHeader = key === 'set-cookie';
        if (isSetCookieHeader) {
            headers[key] ??= [];
            headers[key].push(value);
            return;
        }
        headers[key] = value;
    });
    return headers;
};
exports.parseResponseHeaders = parseResponseHeaders;
const pickResponseJsonData = (jsonResponse) => {
    return {
        url: jsonResponse.url_effective,
        ipAddress: jsonResponse.remote_ip,
        port: jsonResponse.remote_port,
        statusCode: jsonResponse.http_code,
    };
};
exports.pickResponseJsonData = pickResponseJsonData;
//# sourceMappingURL=utils.js.map