import { ParsedResponseJsonData, ParsedSingularResponse } from './parsed-response';
import { PRESETS } from '../presets';
export type BrowserPresets = keyof typeof PRESETS;
interface CurlImpersonateOptions {
    method: string;
    debugLogger: (message: string) => void;
    headers: object;
    /**
     * If `false`, headers from preset file are not used, only custom ones
     * @default true
     */
    usePresetHeaders?: boolean;
    flags?: Array<string>;
    body?: object | string;
    /**
     * Timeout in which the request needs to finish, given in seconds.
     * @default 30
     */
    timeoutSecs?: number;
    followRedirects?: boolean | true;
    impersonate?: BrowserPresets;
    /**
     * List of browsers to impersonate, they will be randomly selected
     * - If set, overrides `impersonate` option
     */
    impersonateList?: BrowserPresets[];
    /**
     * If set, the request will be made using HTTP/2 protocol. All browser presets use HTTP/2 by default.
     * - Otherwise, HTTP/1.1 will be used.
     * - Assigns the `--http2` flag in cURL.
     *
     * @default true
     */
    useHttp2?: boolean;
    /**
     * If set, the request will be compressed. All browser presets use compression by default.
     * - Assigns the `--compressed` flag in cURL.
     *
     * @default true
     */
    useRequestCompression?: boolean;
}
interface CurlResponse extends ParsedResponseJsonData, ParsedSingularResponse {
}
interface CurlShellOutput {
    stdout: string;
    stderr: string;
}
export { CurlImpersonateOptions, CurlResponse, CurlShellOutput };
//# sourceMappingURL=interfaces.d.ts.map