/**
 * Fully parsed output for a single URL request
 */
export type ParsedResponse = ParsedResponseJsonData & ParsedSingularResponse;
/**
 * Parsed for a singular in multiple parallel requests. Part of full single request output
 */
export type ParsedSingularResponse = {
    responseHeaders: {
        [headerKey: string]: string;
    } & {
        'set-cookie'?: string[];
    };
    response: string;
};
/**
 * Parsed JSON data for a single request
 */
export type ParsedResponseJsonData = {
    url: string;
    ipAddress: string;
    port: number;
    statusCode: number;
};
/**
 * Object containing the file name so that the data can be matched together. Used for multiple parallel requests.
 */
type FileName = {
    file: string;
};
export type ParsedResponseJsonDataFile = ParsedResponseJsonData & FileName;
export type ParsedSingularResponseFile = ParsedSingularResponse & FileName;
export type SingularResponseFile = FileName & {
    response: string;
};
/**
 * The parsed headers object.
 * - The `set-cookie` header is different as it's an array of strings (there can be multiple `set-cookie` headers).
 */
export type ParsedHeaders = {
    [headerKey: string]: string;
} & {
    'set-cookie'?: string[];
};
export {};
//# sourceMappingURL=parsed-response.d.ts.map