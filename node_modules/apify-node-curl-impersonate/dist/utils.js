"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRandomSeparatorString = exports.getTimeout = void 0;
exports.escapeUrl = escapeUrl;
/**
 * Escapes and cleans the URL. The URL is escaped and normalized to a valid URL.
 * - We especially need to escape single quotes (`'`), as they are used in the curl shell command.
 * - Quotes in the path are escaped for shell specifically using `'\''`. see https://stackoverflow.com/a/1250279
 */
function escapeUrl(url) {
    const normalizedUrl = new URL(url).toString();
    const SHELL_ESCAPE_QUOTE = "'\\''";
    return normalizedUrl.replace(/'/g, SHELL_ESCAPE_QUOTE);
}
/**
 * Returns a timeout and it's promise that resolves after the given time.
 * - The returned timeout object should be cleared on an upper scope if it's no longer needed.
 */
const getTimeout = (timeoutMs) => {
    const { promise: timeoutPromise, resolve, reject } = getPromiseWithResolve();
    const timeout = setTimeout(resolve, timeoutMs);
    return {
        cancelTimeout: () => {
            clearTimeout(timeout);
            reject();
        },
        timeoutPromise,
    };
};
exports.getTimeout = getTimeout;
/**
 * Returns a promise and its resolve function. The resolve function can be called from outside to resolve the promise.
 *
 * This is Promise.withResolvers, but we won't get that until Node 22
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/withResolvers#browser_compatibility
 */
const getPromiseWithResolve = () => {
    let resolve;
    let reject;
    const promise = new Promise((res, rej) => {
        resolve = res;
        reject = rej;
    });
    return { promise, resolve: resolve, reject: reject };
};
/**
 * Returns a random short string that can be used as an ID, separator, etc.
 */
const getRandomSeparatorString = () => {
    return Math.random().toString(36).substring(2);
};
exports.getRandomSeparatorString = getRandomSeparatorString;
//# sourceMappingURL=utils.js.map