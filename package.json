{
  "type": "module",
  "dependencies": {
    "@hono/node-server": "^1.19.13",
    "discord.js": "^14.26.2",
    "dotenv": "^17.4.1",
    "entities": "^8.0.0",
    "hono": "^4.12.12",
    "httpcloak": "^1.6.1",
    "lavalink-client": "^2.10.0",
    "linkedom": "^0.18.12",
    "tslib": "^2.8.1",
    "undici": "^8.0.2",
    "x-client-transaction-id": "^0.2.0"
  },
  "scripts": {
    "start": "pm2 delete api 2>/dev/null; pm2 start \"bun .\" --name api -i 1 && pm2 save",
    "start-4": "pm2 delete api 2>/dev/null; pm2 start \"NODE_OPTIONS='--dns-result-order=ipv4first' bun .\" --name api -i 1 && pm2 save",
    "start-6": "pm2 delete api 2>/dev/null; pm2 start \"NODE_OPTIONS='--dns-result-order=ipv6first' bun .\" --name api -i 1 && pm2 save",
    "stop": "pm2 delete api 2>/dev/null",
    "gen": "bun gen-routes.ts",
    "gen-update": "rm -rf node_modules bun.lock functions routes && curl -L https://github.com/GreenVGJR/api/tarball/main | tar -xz --strip-components=1 && bun install && bun update --latest && bun run gen && npm install -g pm2 && bun run start"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "bun-types": "^1.3.11",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
